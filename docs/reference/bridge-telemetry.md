# Bridge Telemetry

`easyeda_bridge_status` now includes an optional `diagnostics` object with local connection timing, selected port, registry hash, and optional reconnect details. The data is read-only and helps debug stale bridge connections.
